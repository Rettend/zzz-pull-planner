---
trigger: always_on
---

# IMPORTANT

- Ignore lint errors and warnings! They are auto-fixed on file save, just forget about them
- Don't use `console.warn` ever. the `console.log` eslint warnings are expected, they are a reminder to remove them after debugging
- Don't run the dev server yourself
